The local gate, `{{.Gate}}`, failed on your work. {{.Why}}

Its output is in `.git/afk-gate.log`. Fix the cause and commit the fix to
`{{.Branch}}`. The same rules hold as before: nobody is here to answer a
question, do not push or change branches, and leave nothing uncommitted.

Reply with a short summary of what you changed and why.
